#include <stdio.h>
#include <string.h>

int count_the(char *string);

int main(void){

}

int count_the(char *string){
	char *pts = string;
	int count = 0;
	int i;
	while(*pts != '\0'){
		if(*pts == 't'){

		}
	}
}